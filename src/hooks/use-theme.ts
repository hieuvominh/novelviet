'use client';

import { useTheme } from '@/components/providers/theme-provider';

/**
 * Custom hook for using theme functionality
 * Re-exports the useTheme hook for convenient access
 */
export { useTheme };
